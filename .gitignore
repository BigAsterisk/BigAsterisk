# Build output
target/
project/target/
project/project/
.bsp/

# Project-local toolchain, fetched by bin/bootstrap
tools/

# Generated example data and harness output. Only the files generate.py writes: the
# small fixtures beside it are committed, because the platform tour reads them.
examples/data/airport.csv
examples/data/weather.csv
examples/data/student.txt
tpcds/data/
tpcds/ablation.csv
*.classpath

# Benchmark harness output
benchmarks-results.md
benchmarks-results.csv

# Docs build
site/
docs/api/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Logo sources. The variants the site and the README actually use are committed under
# docs/assets/; the working directory they come from is not. Anchored, so it does not
# also swallow docs/assets/.
/assets/

# Editors / OS
.idea/
.vscode/
.DS_Store
